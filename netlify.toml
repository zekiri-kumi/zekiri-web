# Netlify: build y publicación para Astro con adapter Netlify (SSR + API).
[build]
  command = "npm run build"
  publish = "dist"

# Alias común por typo / SEO
[[redirects]]
  from = "/logistca"
  to = "/logistica"
  status = 301
